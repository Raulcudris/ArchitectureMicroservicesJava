server:
  port: 8084

spring:
  application:
    name: UserServiceApplication
  datasource:
    url: jdbc:mysql://localhost:3306/shoppingstore
    username: admin
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
  mysql:
    url: jdbc:mysql://localhost:3306/shoppingstore
    username: admin
    password: root
jwt:
  secret: mySecretKey
  expirationMs: 3600000  # 1 hora
